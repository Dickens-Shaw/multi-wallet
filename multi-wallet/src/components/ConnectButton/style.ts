import { styled } from 'styled-components';

const WalletInfoInfoWrap = styled.div`
	display: flex;
	align-items: center;

	button {
		display: flex;
		align-items: center;
		gap: 10px;
	}
`;

export default WalletInfoInfoWrap;
